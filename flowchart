Documents → Chunking → Embeddings → Vector DB
                                    ↑
User Query → Embedding → Retrieval → Context
                                    ↓
                              LLM → Answer